# AI 电台竞品差距与迭代建议

> 历史路线文档：下述差距形成于推荐闭环和真实频谱落地前。2026-08 当前实现状态以 `README.md`、`ai-radio-plan.md` 和 `CODE_REVIEW_REPORT.md` 为准。

## 一句话结论

TuanRadio 已经具备播放器、AI DJ、TTS、多音源搜索、收藏和视觉反馈基础，但要接近“好听的 AI 电台”，关键不是继续堆功能，而是形成推荐闭环：理解用户一句话需求，生成少量高质量候选节目单，解释推荐理由，播放后根据反馈调整下一批。

## 当前优势

- 播放核心已经稳定，支持本地和在线音源。
- AI DJ 能聊天、串场、TTS 播报，并可执行播放控制。
- 收藏和播放列表已经有持久化基础。
- 多音源搜索提供了推荐候选池。
- 复古电台 UI、星空和频谱反馈已经有产品气质雏形。

## 原主要差距的当前状态

- 推荐已从“下一首”升级为当前会话节目单，支持可播状态、去重和 DJ 单首 fallback。
- 推荐标签保留在节目单卡片，整体推荐理由通过 DJ 气泡表达。
- NOPE、CALM、FIRE 和 `change_mood` 已影响当前会话推荐。
- `RadioState` 已统一推荐、搜索、TTS、播放和错误状态。
- SongStory 已形成现曲 LLM 讲述 + TTS 的独立入口。
- 频谱已接入 WasapiLoopbackCapture + 1024 点 FFT；捕获不可用时才使用播放态视觉兜底。

当前主要差距转为长期用户画像、外部音源长期可用性、浅色主题视觉完成度和自动化耐久测试。

## 迭代顺序

1. P0：清理旧方向和乱码，保证代码和文档一致。
2. P1：实现推荐节目单、当前会话反馈和 Radio Mode 节目单消耗。
3. P2：完善节目单 UI、JSON 控制协议和统一状态反馈。
4. P3：加入 Song Story、真实频谱评估和外部音源韧性。

## 推荐服务目标

推荐服务输入：

- 用户自然语言意图。
- 当前曲目。
- 收藏和播放列表。
- 当前会话反馈。
- 排除曲目。

推荐服务输出：

- 3-5 首候选节目单。
- 每首歌的来源、可播放状态、短标签和推荐理由。
- 失败时清晰返回状态，不影响播放器稳定性。

## UI 建议

- 当前节目单、收藏、搜索分开表达。
- 当前播放候选高亮。
- 卡片只放歌名、歌手、来源、标签、可播状态。
- 推荐理由主要由 DJ 气泡解释。
- 顶部和底部保持 `ON AIR` / `CONNECTED` / `PLAYING` / `SPEAKING` 等电台状态。

## 后续风险

- 外部音乐 API 容易变更，需要持续用 mock 覆盖空结果、超时、格式变化。
- 推荐服务不能依赖 LLM 总是返回好格式，必须有 fallback query。
- TTS 和自动切歌要继续避免打断用户手动播放。
