# AI 电台竞品差距与迭代建议

## 一句话结论

AIRadio 已经具备播放器、AI DJ、TTS、多音源搜索、收藏和视觉反馈基础，但要接近“好听的 AI 电台”，关键不是继续堆功能，而是形成推荐闭环：理解用户一句话需求，生成少量高质量候选节目单，解释推荐理由，播放后根据反馈调整下一批。

## 当前优势

- 播放核心已经稳定，支持本地和在线音源。
- AI DJ 能聊天、串场、TTS 播报，并可执行播放控制。
- 收藏和播放列表已经有持久化基础。
- 多音源搜索提供了推荐候选池。
- 复古电台 UI、星空和频谱反馈已经有产品气质雏形。

## 主要差距

- 推荐还需要从“下一首”升级为“本轮节目单”。
- 推荐理由和标签尚未在 UI 中成为清晰的信息层。
- 用户反馈动作还没有完整影响后续推荐。
- 状态反馈需要统一，覆盖推荐、搜索、TTS、播放失败。
- 单曲讲述和节目氛围还未形成独立体验。
- 频谱仍是模拟数据，需要明确标注或后续接入真实 FFT。

## 迭代顺序

1. P0：清理旧方向和乱码，保证代码和文档一致。
2. P1：实现推荐节目单、当前会话反馈和 Radio Mode 节目单消耗。
3. P2：完善节目单 UI、JSON 控制协议和统一状态反馈。
4. P3：加入 Song Story、真实频谱评估和外部音源韧性。

## 推荐服务目标

推荐服务输入：

- 用户自然语言意图。
- 当前曲目。
- 收藏和播放列表。
- 当前会话反馈。
- 排除曲目。

推荐服务输出：

- 3-5 首候选节目单。
- 每首歌的来源、可播放状态、短标签和推荐理由。
- 失败时清晰返回状态，不影响播放器稳定性。

## UI 建议

- 当前节目单、收藏、搜索分开表达。
- 当前播放候选高亮。
- 卡片只放歌名、歌手、来源、标签、可播状态。
- 推荐理由主要由 DJ 气泡解释。
- 顶部和底部保持 `ON AIR` / `CONNECTED` / `PLAYING` / `SPEAKING` 等电台状态。

## 后续风险

- 外部音乐 API 容易变更，需要持续用 mock 覆盖空结果、超时、格式变化。
- 推荐服务不能依赖 LLM 总是返回好格式，必须有 fallback query。
- TTS 和自动切歌要继续避免打断用户手动播放。
